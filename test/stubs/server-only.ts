// Test stub for the `server-only` package. In production `server-only` throws
// if imported into a client bundle; under Vitest (Node) we just want a no-op so
// the server modules can be exercised directly.
export {}
